class SearchParams < BitHash
  
  # first set base
  # Integer up to 62
  # string
  # array
  set_base 62 

  field :segment,   :values => ['stuff']
  field :min_price, :values => 5, :characters => 1
  field :infinity,  :values => a, :bits => -1
end
